#include "syscall.h"
#include <stdio.h> // Include for printf function

int main() {
    int result;
    result = Sub(43, 23);
    printf("Result of subtraction: %d\n", result); // Print the result
    Halt();
}
